<% for(drive of drives){ %>
<div class="card" style="width: 18rem;">
    <div class="card-body">
        <%= drives.length %>
        <h5 class="card-title"><%= drive.drive_id.companyName %></h5>
        <ul class="list-group list-group-flush">
            <li class="list-group-item">Next Round :
                <%= drive.drive_id.selectionProcess[ drive.stage - 1 ] %> </li>
        </ul>
        <div class="card-footer">
            <% if (drive.status === 'pending') { %>
            <a href="#" class="btn btn-sm btn-secondary pending">Pending</a>
            <% } else if(drive.status === 'accepted'){%>
            <a href="#" class="btn btn-sm btn-success accepted">Accepted</a>
            <% }else if(drive.status === 'rejected'){ %>
            <a href="#" class="btn btn-sm btn-danger rejected">Rejected</a>
            <% } %>

            <a href="#" class="btn btn-info">Track</a>
        </div>
    </div>
</div>
<% } %>



//R Profile
//one
<% if (Rid === s._id.toString()) { %>
<a href="/dashboard" class="btn btn-primary mb-2">Dashboard</a>
<a href="/profile/<%= s._id %>/edit" class="btn btn-secondary mb-2">Edit</a>
<a href="/logout" class="btn btn-danger">Log out</a>
<% } else { %>
<button class="btn btn-info">Resume</button>
<% } %>



//All drive main
<main class=" container p-1  ">
    <%- include('../partials/flashPartials.ejs') %>
    <% if(drives.length < 1){ %>
    <div class="container d-flex justify-content-center align-items-center flex-column bg-body-secondary rounded-2"
        style="height: calc(100vh - 150px);">
        <h3>No On Going Drive Yet</h3>
        <form action="#" class="text-start  ">
            <button type="submit" class="btn btn-info btn-sm">Notify Me</button>
            <p class="form-text">notify me when a drive gets started.</p>
        </form>
    </div>
    <% }else{ %>
    <h1>Upcoming Drives</h1>
    <div class="container d-flex gap-3 flex-wrap">
        <% for (drive of drives) {%>
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h4 class="card-title"><%= drive.companyName %></h4>
                <h5 class="card-title"><%= drive.jobTitle %></h5>
                <p class="card-text"><%= drive.jobDescription.substring(0, 50) %>...
                </p>
            </div>
            <ul class="list-group list-group-flush">
                <li class="list-group-item"><%= drive.salary %></li>
                <li class="list-group-item"><%= drive.driveDate.toDateString() %></li>
            </ul>
            <div class="card-body">

                <% if(drive.status === 'not-started'){ %>
                <button class="btn btn-sm btn-outline-secondary " disabled>Not Started</button>
                <% }else if(drive.status === 'active'){ %>
                <button class="btn btn-sm btn-outline-success  " disabled>Active</button>
                <% } %>


                <a href="/drive/<%= drive._id %>" class="btn btn-sm btn-primary float-end ">Read more</a>
            </div>
        </div>
        <% } %>
    </div>
    <% } %>
</main>




/Apply Drives
//NEW
<% if(currentDrive.status === 'deactivated'){ %>

<div class="buttons">
    <button class="button" style="background-color: #878787;" disabled>Terminated</button>
</div>

<% }else if(currentDrive.status === 'active'){%>
<b><span class="fs-5">Applications to this drive are closed</span></b>

<% }else if(currentDrive.author.equals(currentUser.Rid)) {%>
<div class="buttons">
    <form action="/drive/<%= currentDrive._id %>?_method=DELETE" method="post">
        <button class="button delete-button" type="submit" role="button">Delete</button>
    </form>
    <a href="/drive/<%= currentDrive._id %>/edit" class="button">Edit</a>
</div>

<% } else if(currentDrive.appliedStudents.map(s => s.toString()).includes(currentUser.sID.toString())){%>
<div class="buttons">
    <button disabled="disabled" class="btn btn-secondary px-3">Applied</button>
    <a href="" class="btn  btn-info">Track Application</a>
</div>
<% }else{ %>
<div class="buttons">
    <form action="/apply/<%= currentDrive._id %>" method="post">
        <button class="button " role="button">Apply</button>
    </form>
</div>
<% } %>

//OLD
<% if(currentDrive.author.equals(currentUser.Rid)) {%>
<% if(currentDrive.status === 'deactivated'){ %>
<div class="buttons">
    <button class="button" style="background-color: #878787;" disabled>Terminated</button>
</div>
<% }else{ %>
<div class="buttons">
    <form action="/drive/<%= currentDrive._id %>?_method=DELETE" method="post">
        <button class="button delete-button" type="submit" role="button">Delete</button>
    </form>
    <a href="/drive/<%= currentDrive._id %>/edit" class="button">Edit</a>
</div>
<% } %>

<% } else if(currentDrive.appliedStudents.map(s => s.toString()).includes(currentUser.sID.toString())){%>
<div class="buttons">
    <button disabled="disabled" class="btn btn-secondary px-3">Applied</button>
    <a href="" class="btn  btn-info">Track Application</a>
</div>
<% }else if(currentDrive.status === 'active'){%>
<b><span class="fs-5">Applications to this drive are closed</span></b>
<% }else{ %>
<div class="buttons">
    <form action="/apply/<%= currentDrive._id %>" method="post">
        <button class="button " role="button">Apply</button>
    </form>
</div>
<% } %>


//OLD landing-page
<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Placement Cell</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
            integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    </head>

    <body>

        <main class="container-fluid ">

            <h1>This is the Landing Page</h1>
            <div>
                <a href="/student/register">Student</a>
                <br>
                <a href="/drive/register">Recruiter</a>
                <br>
                <a href="/drive/all">All drives</a>
                <br>
                <br>
                <a href="/dashboard">Dashboard</a>
                <br>
                <div>
                    <% if(!currentUser){ %>
                    <a href="/login">login</a>
                    <br>
                    <a href="/register">register</a>
                    <br>
                    <% }else{%>
                    <b><a href="#"><%= currentUser.username %></a></b>
                    <a href="/logout">logout</a>
                    <% } %>
                </div>
            </div>
        </main>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
    </body>

</html>